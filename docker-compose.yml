services:
  backend:
    image: movingju/test:pigo_rec
    ports:
      - "8080:8080"
    environment:
      - UVICORN_RELOAD=false
    tty: true
    networks:
      - server-net
    volumes:
      - ./log:/app/log

networks:
  server-net: